on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
    - name: Hello world action step
      id: hello
      uses: vinay0410/tectonic-action@master
      with:
        tex-path: 'resume/myResume.xtx'